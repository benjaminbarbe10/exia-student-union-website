{% extends "BDEShopBundle::layoutShop.html.twig" %}

{% block title %}
    Acheter un  - {{ parent() }}
{% endblock %}

{% block bodyshop %}
    <div style="margin-left: 10%;">
        <a class="btnAddEvent" href="{{ path('bde_shop_index') }}"><</a>
    </div>
    <div class="product group">
        <div Class="col-1-2 product-image">
            <img src={{ asset(article.picture) }}>
            <div class="bg"></div>
            <div class="indicator">
                <div class="dot one"></div>
                <div class="dot two"></div>
                <div class="dot three"></div>
                <div class="dot four"></div>
                <div class="dot five"></div>
            </div>
        </div>


        <div class="col-1-2 product-info">
            <h1>{{ article.name }}</h1>
            <h2>{{ article.priceTTC }} €</h2>

            <div class="button-add-cart">
                <form action="{{ path('bde_shop_view', {'id': article.id}) }}" method="post" {{ form_start(form) }}

                {{ form_errors(form) }}
                {{ form_row(form.add) }}
                {#<a href="" class="add-btn">Ajouter au panier</a>#}

                </form>
            </div>

            <p>{{ article.description }}</p>
            <ul>
                <li>Pull 100% coton</li>
                <li>Disponible en rouge Bordeaux / gris clair</li>
                <li>Capuche à cordon de serrage</li>
            </ul>
        </div>
    </div>
{% endblock %}
